# API Contracts

**대상**: `apps/api` REST API. 클라이언트는 `apps/web`(대시보드)이며, 인증된 사용자만 호출한다.

- **계약 형식**: 리소스별 엔드포인트·메서드·요청/응답 요약. 상세 스키마는 구현 시 FastAPI에서 OpenAPI(Swagger) 자동 생성으로 제공한다.
- **인증**: 세션·JWT 등(구현 단계에서 결정). 모든 API는 인증 필요(인증 제외 경로 별도 정의).

파일 구성:
- `api.md`: 리소스·엔드포인트·주요 DTO 요약.
